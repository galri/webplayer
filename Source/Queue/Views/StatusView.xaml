<UserControl x:Class="Webplayer.Modules.Structure.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Webplayer.Modules.Structure.Views"
             xmlns:mvvm="http://prismlibrary.com/"
             mc:Ignorable="d" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mvvm:ViewModelLocator.AutoWireViewModel="True">
    <materialDesign:ColorZone materialDesign:ShadowAssist.ShadowDepth="Depth2" Height="40"
                                          Mode="PrimaryMid" DockPanel.Dock="Top">

        <Grid>
            <Grid Width="220">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Command="{Binding PreviousCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}" Width="24" Height="24" Content="Prev"></Button>
                <Button Grid.Column="1" Command="{Binding PlayPauseCommand}" Width="28" Height="28">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFloatingActionMiniLightButton}">
                            <Setter Property="Content" Value="Pau"></Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPlaying}" Value="false">
                                    <Setter Property="Content" Value="Play"></Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Width="24" Height="24" Grid.Column="2" Command="{Binding NextCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}">Next</Button>
            </Grid>
        </Grid>
    </materialDesign:ColorZone>
</UserControl>
